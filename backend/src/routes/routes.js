const { Router } = require('express');
const routes = Router();

//importação do controller
const controller = require ('../controllers/controller');


// routes.metodo('/rota', controller.funcao);

module.exports = routes;